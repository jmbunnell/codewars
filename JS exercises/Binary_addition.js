// Adds two parameters together and returns binary conversion
function addBinary(a,b){
    // Summed the numbers & converted it to string with a 2 parameter so it would only be 0 & 1 (binary code)
    return (a+b).toString(2)
}


